const PublicRoutes = {
  HOME: "/",
  LOGIN: "/login",
  REGISTER: "/register",
  WEBSITES: "/websites",
  EDITOR: "/editor",
  BUILDER: "/builder",
};

const PrivateRoutes = {
  WEBSITES: "/websites",
  BUILDER: "/builder",
};

export { PublicRoutes, PrivateRoutes };
